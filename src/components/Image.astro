---
interface Props {
  src: string;
  alt: string;
  width: string;
  height: string;
}

const { src, alt, width, height } = Astro.props;
---

<img src={src} alt={alt} width={width} height={height} />
<style>
  img {
    display: block;
    margin: 0 auto;
  }
</style>